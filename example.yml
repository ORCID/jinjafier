# test comment
org:
  wibble:
    testCamelCaps: 20
    test:
      long:
        property: "this is a long prop that is space separated to test whether wrapping occurs in the go yaml lib"

    # testing = in value
    brokerURL: tcp://wibble:61616?jms.useAsyncSend=true

    # testing - in key
    message-listener:
      retryCount: 3

    # testing weird cron
    cronFormat: "*/5 * * * * *"
